{
  "title": "RAG 项目规范",
  "content": "RAG 系统用于把企业内部文档转换为可检索知识库。文档进入系统后，需要先保存原始内容，再进行切块处理，随后通过 embedding 模型生成向量并写入向量库。问答时，系统先根据用户问题检索相似片段，再将片段作为上下文交给大模型。每一次调用都必须记录问题、检索结果、模型输入、模型输出、输入 token、输出 token 和总 token，便于成本统计、问题排查和效果评估。MySQL 负责保存结构化业务数据和调用日志，Milvus 负责保存向量数据并执行相似度检索。"
}
